{
  "name": "joshapproved-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "sync-design-system": "node scripts/sync-design-system.mjs",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "astro": "^5.0.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.87.0"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12"
  }
}
